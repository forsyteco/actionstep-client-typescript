// This file is auto-generated by @hey-api/openapi-ts

export const PagedQuickCodesSchema = {
    type: 'object',
    properties: {
        QuickCodes: {
            type: 'array',
            items: {
                '$ref': '#/components/schemas/QuickCode'
            }
        },
        meta: {
            '$ref': '#/components/schemas/PageMetaData'
        }
    }
} as const;

export const PageMetaDataSchema = {
    type: 'object',
    properties: {
        paging: {
            '$ref': '#/components/schemas/PagingData'
        }
    }
} as const;

export const PagingDataSchema = {
    type: 'object',
    properties: {
        QuickCodes: {
            '$ref': '#/components/schemas/QuickCodesPageData'
        }
    }
} as const;

export const QuickCodesPageDataSchema = {
    type: 'object',
    properties: {
        recordCount: {
            description: 'The total number of Quick Code records returned by the underlying query.',
            type: 'integer',
            example: 2487
        },
        pageCount: {
            description: 'The total number of pages generated by the underlying query.',
            type: 'integer',
            example: 50
        },
        page: {
            description: 'The page number for this page of Quick Codes.',
            type: 'integer',
            example: 2
        },
        pageSize: {
            description: 'Page size.',
            type: 'integer',
            example: 50
        },
        prevPage: {
            description: 'A URL to the previous page of Quick Codes.',
            type: 'string',
            example: 'https://ap-southeast-2.actionstep.com/api/rest/quickcodes?page=1'
        },
        nextPage: {
            description: 'A URL to the next page of Quick Codes.',
            type: 'string',
            example: 'https://ap-southeast-2.actionstep.com/api/rest/quickcodes?page=3'
        }
    }
} as const;

export const QuickCodeSchema = {
    type: 'object',
    properties: {
        id: {
            description: 'Unique identifier (code) for the Quick Code.',
            type: 'integer',
            format: 'integer',
            example: 300,
            readOnly: true
        },
        quickCode: {
            description: 'The value for the quick code.',
            type: 'string',
            format: 'string',
            example: 'Client Communications'
        },
        description: {
            description: 'A description for the quick code.',
            type: 'string',
            format: 'string',
            example: 'For all client communications.'
        },
        useForRetainerTransfers: {
            description: 'Indicates if the quick code is available for retainer transfers.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForReceipts: {
            description: 'Indicates if the quick code is available for receipts.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForTasks: {
            description: 'Indicates if the quick code is available for tasks.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForTimeEntry: {
            description: 'Indicates if the quick code is available for time entry.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForWithdrawals: {
            description: 'Indicates if the quick code is available for withdrawals.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        links: {
            '$ref': '#/components/schemas/QuickCodeLinks'
        }
    }
} as const;

export const QuickCodeLinksSchema = {
    type: 'object',
    properties: {
        rate: {
            description: 'Unique identifier for a rate associated with the quick code.',
            example: 546,
            type: 'integer',
            readOnly: true
        }
    }
} as const;

export const CreateQuickCodeSchema = {
    type: 'object',
    required: ['quickCode', 'description'],
    properties: {
        quickCode: {
            description: 'The value for the quick code.',
            type: 'string',
            format: 'string',
            example: 'Client Communications'
        },
        description: {
            description: 'A description for the quick code.',
            type: 'string',
            format: 'string',
            example: 'For all client communications.'
        },
        useForRetainerTransfers: {
            description: 'Indicates if the quick code is available for retainer transfers.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForReceipts: {
            description: 'Indicates if the quick code is available for receipts.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForTasks: {
            description: 'Indicates if the quick code is available for tasks.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForTimeEntry: {
            description: 'Indicates if the quick code is available for time entry.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForWithdrawals: {
            description: 'Indicates if the quick code is available for withdrawals.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        links: {
            '$ref': '#/components/schemas/CreateQuickCodeLinks'
        }
    }
} as const;

export const CreateQuickCodeLinksSchema = {
    type: 'object',
    properties: {
        rate: {
            description: 'Unique identifier for a rate associated with the quick code.',
            example: 546,
            type: 'integer',
            readOnly: true
        }
    }
} as const;

export const UpdateQuickCodeSchema = {
    type: 'object',
    properties: {
        quickCode: {
            description: 'The value for the quick code.',
            type: 'string',
            format: 'string',
            example: 'Client Communications'
        },
        description: {
            description: 'A description for the quick code.',
            type: 'string',
            format: 'string',
            example: 'For all client communications.'
        },
        useForRetainerTransfers: {
            description: 'Indicates if the quick code is available for retainer transfers.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForReceipts: {
            description: 'Indicates if the quick code is available for receipts.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForTasks: {
            description: 'Indicates if the quick code is available for tasks.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForTimeEntry: {
            description: 'Indicates if the quick code is available for time entry.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForWithdrawals: {
            description: 'Indicates if the quick code is available for withdrawals.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        links: {
            '$ref': '#/components/schemas/UpdateQuickCodeLinks'
        }
    }
} as const;

export const UpdateQuickCodeLinksSchema = {
    type: 'object',
    properties: {
        rate: {
            description: 'Unique identifier for a rate associated with the quick code.',
            example: 546,
            type: 'integer',
            readOnly: true
        }
    }
} as const;

export const PagedQuickCodesWritableSchema = {
    type: 'object',
    properties: {
        QuickCodes: {
            type: 'array',
            items: {
                '$ref': '#/components/schemas/QuickCodeWritable'
            }
        },
        meta: {
            '$ref': '#/components/schemas/PageMetaData'
        }
    }
} as const;

export const QuickCodeWritableSchema = {
    type: 'object',
    properties: {
        quickCode: {
            description: 'The value for the quick code.',
            type: 'string',
            format: 'string',
            example: 'Client Communications'
        },
        description: {
            description: 'A description for the quick code.',
            type: 'string',
            format: 'string',
            example: 'For all client communications.'
        },
        useForRetainerTransfers: {
            description: 'Indicates if the quick code is available for retainer transfers.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForReceipts: {
            description: 'Indicates if the quick code is available for receipts.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForTasks: {
            description: 'Indicates if the quick code is available for tasks.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForTimeEntry: {
            description: 'Indicates if the quick code is available for time entry.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForWithdrawals: {
            description: 'Indicates if the quick code is available for withdrawals.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        }
    }
} as const;

export const CreateQuickCodeWritableSchema = {
    type: 'object',
    required: ['quickCode', 'description'],
    properties: {
        quickCode: {
            description: 'The value for the quick code.',
            type: 'string',
            format: 'string',
            example: 'Client Communications'
        },
        description: {
            description: 'A description for the quick code.',
            type: 'string',
            format: 'string',
            example: 'For all client communications.'
        },
        useForRetainerTransfers: {
            description: 'Indicates if the quick code is available for retainer transfers.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForReceipts: {
            description: 'Indicates if the quick code is available for receipts.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForTasks: {
            description: 'Indicates if the quick code is available for tasks.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForTimeEntry: {
            description: 'Indicates if the quick code is available for time entry.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForWithdrawals: {
            description: 'Indicates if the quick code is available for withdrawals.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        }
    }
} as const;

export const UpdateQuickCodeWritableSchema = {
    type: 'object',
    properties: {
        quickCode: {
            description: 'The value for the quick code.',
            type: 'string',
            format: 'string',
            example: 'Client Communications'
        },
        description: {
            description: 'A description for the quick code.',
            type: 'string',
            format: 'string',
            example: 'For all client communications.'
        },
        useForRetainerTransfers: {
            description: 'Indicates if the quick code is available for retainer transfers.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForReceipts: {
            description: 'Indicates if the quick code is available for receipts.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForTasks: {
            description: 'Indicates if the quick code is available for tasks.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForTimeEntry: {
            description: 'Indicates if the quick code is available for time entry.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        },
        useForWithdrawals: {
            description: 'Indicates if the quick code is available for withdrawals.',
            type: 'string',
            enum: ['T', 'F'],
            default: 'T',
            format: 'string',
            example: 'T'
        }
    }
} as const;
