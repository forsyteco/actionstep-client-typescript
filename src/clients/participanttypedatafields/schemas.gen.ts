// This file is auto-generated by @hey-api/openapi-ts

export const PagedParticipantTypeDataFieldsSchema = {
    type: 'object',
    properties: {
        participanttypedatafields: {
            type: 'array',
            items: {
                '$ref': '#/components/schemas/ParticipantTypeDataField'
            }
        },
        meta: {
            '$ref': '#/components/schemas/PageMetaData'
        }
    }
} as const;

export const PageMetaDataSchema = {
    type: 'object',
    properties: {
        paging: {
            '$ref': '#/components/schemas/PagingData'
        }
    }
} as const;

export const PagingDataSchema = {
    type: 'object',
    properties: {
        participanttypedatafields: {
            '$ref': '#/components/schemas/ParticipantTypeDataFieldsPageData'
        }
    }
} as const;

export const ParticipantTypeDataFieldsPageDataSchema = {
    type: 'object',
    properties: {
        recordCount: {
            description: 'The total number of participant type data field records returned by the underlying query.',
            type: 'integer',
            example: 2487
        },
        pageCount: {
            description: 'The total number of pages generated by the underlying query.',
            type: 'integer',
            example: 50
        },
        page: {
            description: 'The page number for this page of participant type data field records.',
            type: 'integer',
            example: 2
        },
        pageSize: {
            description: 'Page size.',
            type: 'integer',
            example: 50
        },
        prevPage: {
            description: 'A URL to the previous page of participant type data field records.',
            type: 'string',
            example: 'https://ap-southeast-2.actionstep.com/api/rest/participanttypedatafields?page=1'
        },
        nextPage: {
            description: 'A URL to the next page of participant type data field records.',
            type: 'string',
            example: 'https://ap-southeast-2.actionstep.com/api/rest/participanttypedatafields?page=3'
        }
    }
} as const;

export const ParticipantTypeDataFieldSchema = {
    type: 'object',
    properties: {
        id: {
            description: 'Unique identifier for a single participant type data field record.',
            type: 'integer',
            format: 'integer',
            example: 7384,
            readOnly: true
        },
        name: {
            description: 'Name of the participant type data field',
            type: 'string',
            format: 'string',
            example: 'Colours'
        },
        dataType: {
            description: 'The data type for the participant type data field.',
            enum: ['Boolean', 'Date', 'DateNM', 'DateTime', 'DateTimeNM', 'DropDown', 'DropDownMulti', 'Money', 'Number', 'Str255', 'Str255Protected', 'TextArea'],
            type: 'string',
            format: 'string',
            example: 'Str255'
        },
        label: {
            description: 'The label for the participant type data field.',
            type: 'string',
            format: 'string',
            example: 'Colour sample'
        },
        formOrder: {
            description: 'The display order for the field when displayed on a user interface.',
            type: 'integer',
            format: 'string',
            example: 4
        },
        required: {
            description: 'Indicates if a value should be provided.',
            enum: ['T', 'F'],
            default: 'F',
            type: 'string',
            format: 'string'
        },
        description: {
            description: 'A descriptor for the participant type data field.',
            type: 'string',
            format: 'string',
            example: 'Car colour'
        },
        synchronizeValues: {
            description: 'When enabled (T) the field value is not unique to any specific Matter, meaning updating this field anywhere will update everywhere at the same time. When disabled (F) each Matter will have a unique value for this field.',
            enum: ['T', 'F'],
            default: 'F',
            type: 'string',
            format: 'string'
        },
        limitUseToMatterOnly: {
            description: 'Enable this only if the field should not be editable on a contact record, but should be editable only on a contact linked to a matter.',
            enum: ['T', 'F'],
            default: 'F',
            type: 'string',
            format: 'string'
        },
        links: {
            '$ref': '#/components/schemas/ParticipantTypeDataFieldLinks'
        }
    }
} as const;

export const ParticipantTypeDataFieldLinksSchema = {
    type: 'object',
    properties: {
        participantType: {
            description: 'Unique identifier for the underlying participant type.',
            example: 8263,
            type: 'integer',
            format: 'string',
            readOnly: true
        },
        tag: {
            description: 'Unique identifier for a tag associated with the data field.',
            example: 392,
            type: 'integer',
            format: 'string',
            readOnly: true
        }
    }
} as const;

export const CreateParticipantTypeDataFieldSchema = {
    type: 'object',
    required: ['name', 'dataType'],
    properties: {
        name: {
            description: 'Name of the participant type data field',
            type: 'string',
            format: 'string',
            example: 'Colours'
        },
        dataType: {
            description: 'The data type for the participant type data field.',
            enum: ['Boolean', 'Date', 'DateNM', 'DateTime', 'DateTimeNM', 'DropDown', 'DropDownMulti', 'Money', 'Number', 'Str255', 'Str255Protected', 'TextArea'],
            default: 'Str255',
            type: 'string',
            format: 'string',
            example: 'Str255'
        },
        label: {
            description: 'The label for the participant type data field.',
            type: 'string',
            format: 'string',
            example: 'Colour sample'
        },
        formOrder: {
            description: 'The display order for the field when displayed on a user interface.',
            type: 'integer',
            format: 'string',
            example: 4
        },
        required: {
            description: 'Indicates if a value should be provided.',
            enum: ['T', 'F'],
            default: 'F',
            type: 'string',
            format: 'string'
        },
        description: {
            description: 'A descriptor for the participant type data field.',
            type: 'string',
            format: 'string',
            example: 'Car colour'
        },
        synchronizeValues: {
            description: 'When enabled (T) the field value is not unique to any specific Matter, meaning updating this field anywhere will update everywhere at the same time. When disabled (F) each Matter will have a unique value for this field.',
            enum: ['T', 'F'],
            default: 'F',
            type: 'string',
            format: 'string'
        },
        limitUseToMatterOnly: {
            description: 'Enable this only if the field should not be editable on a contact record, but should be editable only on a contact linked to a matter.',
            enum: ['T', 'F'],
            default: 'F',
            type: 'string',
            format: 'string'
        },
        links: {
            '$ref': '#/components/schemas/CreateParticipantTypeDataFieldLinks'
        }
    }
} as const;

export const CreateParticipantTypeDataFieldLinksSchema = {
    type: 'object',
    required: ['participantType'],
    properties: {
        participantType: {
            description: 'Unique identifier for the underlying participant type.',
            example: 8263,
            type: 'integer',
            format: 'string'
        },
        tag: {
            description: 'Unique identifier for a tag associated with the data field.',
            example: 392,
            type: 'integer',
            format: 'string'
        }
    }
} as const;

export const UpdateParticipantTypeDataFieldSchema = {
    type: 'object',
    properties: {
        name: {
            description: 'Name of the participant type data field',
            type: 'string',
            format: 'string',
            example: 'Colours'
        },
        label: {
            description: 'The label for the participant type data field.',
            type: 'string',
            format: 'string',
            example: 'Colour sample'
        },
        formOrder: {
            description: 'The display order for the field when displayed on a user interface.',
            type: 'integer',
            format: 'string',
            example: 4
        },
        required: {
            description: 'Indicates if a value should be provided.',
            enum: ['T', 'F'],
            default: 'F',
            type: 'string',
            format: 'string'
        },
        description: {
            description: 'A descriptor for the participant type data field.',
            type: 'string',
            format: 'string',
            example: 'Car colour'
        },
        synchronizeValues: {
            description: 'When enabled (T) the field value is not unique to any specific Matter, meaning updating this field anywhere will update everywhere at the same time. When disabled (F) each Matter will have a unique value for this field.',
            enum: ['T', 'F'],
            default: 'F',
            type: 'string',
            format: 'string'
        },
        limitUseToMatterOnly: {
            description: 'Enable this only if the field should not be editable on a contact record, but should be editable only on a contact linked to a matter.',
            enum: ['T', 'F'],
            default: 'F',
            type: 'string',
            format: 'string'
        },
        links: {
            '$ref': '#/components/schemas/UpdateParticipantTypeDataFieldLinks'
        }
    }
} as const;

export const UpdateParticipantTypeDataFieldLinksSchema = {
    type: 'object',
    required: ['participantType'],
    properties: {
        participantType: {
            description: 'Unique identifier for the underlying participant type.',
            example: 8263,
            type: 'integer',
            format: 'string'
        },
        tag: {
            description: 'Unique identifier for a tag associated with the data field.',
            example: 392,
            type: 'integer',
            format: 'string'
        }
    }
} as const;

export const PagedParticipantTypeDataFieldsWritableSchema = {
    type: 'object',
    properties: {
        participanttypedatafields: {
            type: 'array',
            items: {
                '$ref': '#/components/schemas/ParticipantTypeDataFieldWritable'
            }
        },
        meta: {
            '$ref': '#/components/schemas/PageMetaData'
        }
    }
} as const;

export const ParticipantTypeDataFieldWritableSchema = {
    type: 'object',
    properties: {
        name: {
            description: 'Name of the participant type data field',
            type: 'string',
            format: 'string',
            example: 'Colours'
        },
        dataType: {
            description: 'The data type for the participant type data field.',
            enum: ['Boolean', 'Date', 'DateNM', 'DateTime', 'DateTimeNM', 'DropDown', 'DropDownMulti', 'Money', 'Number', 'Str255', 'Str255Protected', 'TextArea'],
            type: 'string',
            format: 'string',
            example: 'Str255'
        },
        label: {
            description: 'The label for the participant type data field.',
            type: 'string',
            format: 'string',
            example: 'Colour sample'
        },
        formOrder: {
            description: 'The display order for the field when displayed on a user interface.',
            type: 'integer',
            format: 'string',
            example: 4
        },
        required: {
            description: 'Indicates if a value should be provided.',
            enum: ['T', 'F'],
            default: 'F',
            type: 'string',
            format: 'string'
        },
        description: {
            description: 'A descriptor for the participant type data field.',
            type: 'string',
            format: 'string',
            example: 'Car colour'
        },
        synchronizeValues: {
            description: 'When enabled (T) the field value is not unique to any specific Matter, meaning updating this field anywhere will update everywhere at the same time. When disabled (F) each Matter will have a unique value for this field.',
            enum: ['T', 'F'],
            default: 'F',
            type: 'string',
            format: 'string'
        },
        limitUseToMatterOnly: {
            description: 'Enable this only if the field should not be editable on a contact record, but should be editable only on a contact linked to a matter.',
            enum: ['T', 'F'],
            default: 'F',
            type: 'string',
            format: 'string'
        }
    }
} as const;
